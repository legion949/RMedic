## Segmento del UI
rejunteUI <- function(id) {
  ns <- NS(id)
  

}




## Segmento del server
rejunteSERVER <- function(input, output, session, base) {

  
  
  
  
  return(
    list(
      frec_input = reactive({ 3333 }),
      max_input = reactive({ 4444 })
    )
  )
  
  
  #return(batalla_naval()[[1]])
  
  
}


